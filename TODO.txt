MIGRATIONS:

- OpenGLES -> Metal
- Convert all XIBs to autolayout
- Openclipart API -> FreeSVG API (which requires a login to use)

DOCUMENT BROWSER:

- New create panel, with sizes
- New samples panel, with option to import
- New fonts panel, including import button (ttf, otf)
- Camera & photo library import buttons

- Open document: visual feedback
- Should support opening .inkpad, .svg, .svgz (public.svgz-image)

- Action to export

- Test that delete / duplicate work

FEATURES:

- Support dark mode (and remove UIUserInterfaceStyle=Light from Info.plist)
- Multitasking (split view, slide over, multiple windows)
- Quick Look previews for *.inkpad files
